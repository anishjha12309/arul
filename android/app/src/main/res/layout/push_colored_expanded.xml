<?xml version="1.0" encoding="utf-8"?>
<!-- Expanded content of a coloured campaign (push/ArulMessagingService). Android 12+ caps a decorated
     custom view at 252dp expanded: title, up to three body lines and a 140dp picture fit inside it.
     The picture is GONE when the campaign has none or its download missed the deadline. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/push_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#2B3A8A"
    android:orientation="vertical"
    android:paddingStart="12dp"
    android:paddingTop="8dp"
    android:paddingEnd="12dp"
    android:paddingBottom="10dp">

    <TextView
        android:id="@+id/push_title"
        style="@style/TextAppearance.Compat.Notification.Title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="2"
        android:textColor="#FFFFFF" />

    <TextView
        android:id="@+id/push_body"
        style="@style/TextAppearance.Compat.Notification"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:ellipsize="end"
        android:maxLines="3"
        android:textColor="#FFFFFF" />

    <ImageView
        android:id="@+id/push_picture"
        android:layout_width="match_parent"
        android:layout_height="140dp"
        android:layout_marginTop="8dp"
        android:contentDescription="@null"
        android:scaleType="centerCrop"
        android:visibility="gone" />
</LinearLayout>
