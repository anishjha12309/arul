<?xml version="1.0" encoding="utf-8"?>
<!-- Android registers a live wallpaper from this descriptor -> the manifest service points at it -> empty is fine, absent is not. -->
<!-- Users only arrive via EXTRA_LIVE_WALLPAPER_COMPONENT -> the entry is never browsed -> no description or thumbnail. -->
<wallpaper xmlns:android="http://schemas.android.com/apk/res/android" />
